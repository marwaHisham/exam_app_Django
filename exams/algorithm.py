from .models import Course
from .models import Question
from .models import Exam
from .models import Course
from .models import Chapter


class population
    def generateSample (length,question):
	i = 0
	result = []
	while i < length:
		result.append(question)
    return result


    def population(Course,numOfChapter)
        populat=[]
    while i < sizePopulation:
		population.append(generateSample(numOfChapter,)
		i+=1
    return population

